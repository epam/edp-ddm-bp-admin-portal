spring:
  application:
    name: business-process-administration-portal
  cloud:
    kubernetes:
      config:
        enabled: true
        enable-api: false
        paths:
          - /app/config/application.yml
      secrets:
        enabled: true
        enable-api: false
        paths:
          - /app/secrets

---
spring:
  profiles: local
  cloud:
    kubernetes:
      config:
        enabled: false